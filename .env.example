# Copy to .env and fill in. Never commit .env.
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/my_team?schema=public"

# Magic-link auth (phase 1 placeholder — wire up when auth lands)
# AUTH_SECRET=""
# EMAIL_FROM=""
